<h1>Bands</h1>

<% if @bands %>
  <ul>
    <% @bands.each do |band| %>
      <li><%= link_to band.name, band_url(band)%>
    <% end %>
  </ul>
<% end %>

<p><%= link_to "Add Band", new_band_url %></p>
