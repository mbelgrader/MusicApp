<h1>Tracks:</h1>

<% if @tracks %>
  <ul>
    <% @tracks.each do |track| %>
      <li><%= track.name %></li>
    <% end %>
  </ul>
<% end %>

<p><%= link_to "Add Track", new_album_track_url(@album) %></p>
